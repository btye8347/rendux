# Minimal host-app skeleton

Copy this folder into a new project, then add RendUX as a git dependency (see root README).

```bash
# from a new project that depends on rendux@v0.1.0b1
uv run uvicorn main:app --reload --port 8010
```

Open http://127.0.0.1:8010/
