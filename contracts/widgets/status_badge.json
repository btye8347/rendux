{
  "name": "status_badge",
  "status": "verified",
  "interaction": { "profile": "static" },
  "accepts_each": true,
  "props": {
    "status": {
      "type": "enum",
      "enum": ["ok", "warn", "error", "unknown", "info", "default"],
      "required": false
    },
    "label": { "type": "string", "required": false }
  }
}
